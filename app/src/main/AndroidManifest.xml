<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.vogella.android.eventlistenerapp">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_eventlistener"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_eventlistener_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".TheatreActivity" />
        <activity android:name=".ConventionsActivity" />
        <activity android:name=".SportsActivity" />
        <activity android:name=".CommunityActivity" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".ComedyActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>